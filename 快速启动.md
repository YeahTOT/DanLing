## 快速启动

适用于 DanLing v1.1.0。

目前支持 Ultralytics `results.csv`，也支持安装 extra 后读取 TensorBoard event。

### 1. 安装

```bash
pip install danling
```

如果要使用全屏 TUI 和配置 TUI：

```bash
pip install "danling[tui]"
```

如果你是在源码仓库里验证本地未发布改动，不要直接运行系统里的 `danling`；
用当前 worktree 启动：

```bash
PYTHONPATH=src .venv/bin/danling tui logs/run --source csv --config danling.yaml --no-hardware
```

如果要读取 TensorBoard event：

```bash
pip install "danling[tensorboard]"
```

### 2. 初始化配置

```bash
danling init
```

这会在当前目录生成 `danling.yaml`。推荐先用配置 TUI 编辑：

```bash
danling config tui
```

配置 TUI 首页有两个子菜单：

- `1 手动配置`：和普通表单一样，直接填写主指标、baseline/SOTA 和境界阈值。
- `2 自动生成`：先选择 Ultralytics `results.csv` 或 TensorBoard event/logdir，再输入历史路径和关心指标，自动用 20% 位置结果作为 baseline，用历史最好结果作为 SOTA。

最小有效配置：

```yaml
primary_score: mAP50
baseline_score: 0.60
sota_score: 0.80
realm_thresholds:
```

`realm_thresholds` 先留空即可，DanLing 会把 baseline 到 SOTA 自动等分为五个境界。

### 3. 核心操作

先检查一个训练目录或示例目录：

```bash
danling inspect examples/ultralytics_run --json
danling inspect logs/tensorboard --source tensorboard --json
danling status examples/ultralytics_run --config danling.yaml --no-hardware
danling statusline examples/ultralytics_run --config danling.yaml --no-hardware
```

仿真生成一个可监控的 `logs/run/results.csv`：

```bash
danling simulate logs/ultralytics/results.csv logs/run --interval 0 --max-rows 20
danling simulate logs/tensorboard logs/run --source tensorboard --interval 0 --max-rows 20
```

持续监控训练目录：

```bash
danling watch logs/run --source csv --config danling.yaml --no-hardware
```

使用全屏 TUI：

```bash
danling tui logs/run --source csv --config danling.yaml --no-hardware
```

SSH 远程可视化：

```bash
danling remote setup trainbox
danling remote hardware trainbox
danling remote status trainbox --config danling.yaml
danling remote tui trainbox --config danling.yaml
```

`danling remote setup` 会用 TUI 辅助配置 SSH 密钥、远程 `results.csv` 路径、无密码登录测试和远程 GPU 测试。监控阶段强制使用 SSH key，不会等待密码输入，并默认尝试读取远程 NVIDIA GPU 状态。需要跳过远程硬件时加 `--no-remote-hardware`。

常用辅助命令：

```bash
danling config show --config danling.yaml
danling doctor logs/run --source csv --config danling.yaml --no-hardware
danling hardware --json
```

如果没有 GPU/NPU 或硬件命令不可用，先加 `--no-hardware` 跑通训练指标和境界显示。
